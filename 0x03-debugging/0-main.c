#include "main.h"

/**
* main - testing the function that print if integer is positive or negative
* Return: Always 0 (Succes)
*/

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
